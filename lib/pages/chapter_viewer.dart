import 'package:flutter/widgets.dart';

class ChapterViewer extends StatelessWidget {
  const ChapterViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
